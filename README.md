# NexOS

Product docs are at repository root (`PRD.md`, `MVP.md`, `techstack.md`, `flow.md`, `AGENTS.md`).

The runnable web app scaffold is in:

- `./web`

## Run the app

```bash
cd web
npm install
npm run dev
```

Then open `http://localhost:3000`.
